doctype html
html(lang='fr')
    head
        meta(charset='UTF-8')
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/styles/nav", type="text/css")
        block styles

        title Users

    body
        header: include ../__nav
        main
            if toast && toast.statut
              div(class=toast.statut)= toast.message
            block content
        block scripts
        footer