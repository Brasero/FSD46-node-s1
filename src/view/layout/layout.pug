//- Le layout permet de déclarer une structure de base commune à toute les pages (le doctype)
doctype html
html(lang="fr")
    head
        meta(charset="UTF-8")
        title My Layout

        link(rel="stylesheet" href="https://unpkg.com/bootstrap@5.1.3/dist/css/bootstrap.min.css")
        //- le mot clé `block` dans un layout
            permet de définir un block
            à l'emplacement où il est placé
        block styles

        script(src="https://polyfill.io/v3/polyfill.min.js")
        block scripts
    body
        //- `include` permet d'inclure un autre fichier pug
        include ../nav
        div#target
        block content
        block clientScript

        footer © Copyright, Tous droits réservés