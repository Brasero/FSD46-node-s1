mixin menuItem(path, title, isActive)
    - const className = isActive ? 'nav-active' : ""
    li(class=className): a(href=path)= title

nav: ul
    each item in menuItems
      +menuItem(item.path, item.title, item.isActive)